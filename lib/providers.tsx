const Providers = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      {/* Any other providers like Context providers, etc. */}
      {children}
    </>
  );
};

export default Providers;
